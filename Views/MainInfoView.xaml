<UserControl x:Class="VOlkin.Views.MainInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Background="{DynamicResource MaterialDesignPaper}"
             Foreground="{DynamicResource MaterialDesignBody}"
             xmlns:local="clr-namespace:VOlkin.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >

    <UserControl.Resources>
        <DataTemplate x:Key="PaymentTypesTemplate">
            <Grid HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding PaymentTypeName}" Grid.Column="0"/>

                <TextBlock Text="{Binding MoneyAmount}" Grid.Column="1"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="TransactionsTemplate">
            <Grid HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Price}" Grid.Column="0"/>

                <TextBlock Text="{Binding Comment}" Grid.Column="1"/>

                <TextBlock Text="{Binding DateTime}" Grid.Column="2"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer>
        <StackPanel Margin="10">

            <!--PaymentTypes-->
            <Expander>
                <Expander.Header>
                    <Grid  HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Счета" Grid.Column="0" VerticalAlignment="Center">

                        </TextBlock>

                        <TextBlock Text="{Binding Path=TotalMoney, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Column="1" VerticalAlignment="Center">

                        </TextBlock>

                        <Button Grid.Column="2" Command="{Binding AddCardCommand}" Margin="75 0 0 0">
                            <materialDesign:PackIcon Kind="CreditCardPlusOutline" />
                        </Button>

                    </Grid>
                </Expander.Header>

                <ListBox x:Name="PaymenTypesListBox" ItemsSource="{Binding PaymentTypes}" ItemTemplate="{StaticResource PaymentTypesTemplate}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseRightButtonUp">
                            <i:InvokeCommandAction Command="{Binding CloseCardCommand}" CommandParameter="{Binding ElementName=PaymenTypesListBox, Path=SelectedItem}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>
            </Expander>

            <!--Transactions-->
            <Grid HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}" >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel  HorizontalAlignment="Center" Orientation="Horizontal" Grid.Column="1">
                    <TextBlock Text="Доходы и расходы за "
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="16"
                                       />

                    <ComboBox ItemsSource="{Binding TimePeriods}"
                              SelectedItem="{Binding SetCurTimePeriod, Delay=10}"
                              FontSize="16">
                        <ComboBox.Resources>
                            <Style TargetType="{x:Type ComboBoxItem}">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </ComboBox.Resources>

                    </ComboBox>
                </StackPanel>

                <Button Grid.Column="2" HorizontalAlignment="Right" Command="{Binding AddTransactionCommand}">
                    <materialDesign:PackIcon Kind="Plus"/>
                </Button>
            </Grid>

            <ListView ItemsSource="{Binding Transactions}"
                      ItemTemplate="{StaticResource TransactionsTemplate}"/>

        </StackPanel>
    </ScrollViewer>

</UserControl>
